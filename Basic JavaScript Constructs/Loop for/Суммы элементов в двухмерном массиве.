
Напишите функцию с именем sumsInArray, которая принимает двухмерный массив arr в качестве аргумента и возвращает массив, 
содержащий суммы элементов вложенных массивов.

Примеры:

функция sumsInArray([[1, 2], [2, -3, 1, 1], [3, 5, 10], [3, 7]]) должна возвратить [3, 1, 18, 10];
функция sumsInArray([[1, 0, 0], [2, 2]]) должна возвратить [1, 4];
функция sumsInArray([[], [2, 2], [0]]) должна возвратить [0, 4, 0].

function sumsInArray(arr){
let total = [];
  for(let el of arr){
    let sum = 0;
      for(let i = 0; i < el.length; i++){
        sum += el[i]
      }
    total.push(sum);
  }
return total;
}
